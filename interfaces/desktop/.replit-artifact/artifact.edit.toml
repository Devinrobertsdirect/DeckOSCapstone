kind = "web"
previewPath = "/"
title = "Deck OS — JARVIS Command Center"
version = "1.0.0"
id = "artifacts/deck-os"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/deck-os run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/deck-os", "run", "build" ]
publicDir = "artifacts/deck-os/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
